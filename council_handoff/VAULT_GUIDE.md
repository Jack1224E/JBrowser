⚓ JBrowser Vault: Master Guide & Technical Architecture[!IMPORTANT]IMMUTABLE VAULT: This directory (/home/jack/Documents/JBrowser) contains the certified, production-ready state of the Pir Browser suite. Do not "optimize," refactor, or edit the internal paths directly. If changes are required, copy the vault to a development workspace first.1. Core ArchitectureThe JBrowser suite is a self-contained Thorium mod designed for high-speed, intercepted downloads and smart proxy switching.ComponentPathDescriptionBrowserbin/Thorium_Browser...AppImagePortable Thorium Browser (AVX2).Enginebin/aria2cThe high-speed download backbone (RPC enabled).Flutter SDKbin/flutter/Localized SDK for compiling the Switchboard mobile/desktop app.UIUI/dashboard.html"The Meadow" custom start page (Master Dashboard).2. Directory Mappingbin/: Core binaries and local SDKs (Thorium, aria2c, Flutter).UI/: Frontend assets including dashboard.html and the gui/ Electron-bridge/Engine Wrapper interface.extensions/: Unpacked source code for pre-configured browser extensions.pir_switchboard/: The Flutter Native Project (Aria Wrapper source). This is a native application separate from the browser extensions.manifests/: Authorization files, including initial_preferences and the Native Messaging JSON.scripts/: Automation logic, bridge scripts (pir_host.js), and the master launcher.council_handoff/: Historical engineering logs and architectural maps.3. Extension ModsThe browser is hardcoded to load two specific extension IDs authorized in the Native Messaging Host manifest.NameExtension IDPathPir Downloaderjlclojmcifniclnahffcmhocbccljfgcextensions/snatcherPirate Switchboardmfkdafmmppeidkmpnfjminnjimjikiglextensions/switchboardLoading MechanismExtensions are loaded via the --load-extension flag in the launcher script. To bypass Chromium's "space-in-path" bugs, the launcher creates symbolic links in /tmp/pir_suite/ pointing to the extension directories before boot.4. Native Messaging HostThe bridge allows communication between the browser extensions and the aria2c engine.Manifest: manifests/com.pir.browser.engine.jsonLauncher: scripts/pir_host_launcher.sh (executes scripts/pir_host.js).Protocol: Standard Chrome Native Messaging using a 4-byte length header (32-bit UInt32LE).5. Launch ProcedureTo ensure the app runs correctly without "Ghost Tabs," ID mismatches, or profile errors, use the following procedure:Execute the Launcher:Bashbash /home/jack/Documents/JBrowser/scripts/run-pir-browser.sh
Path Logic: The script changes directory (cd) into the JBrowser root to provide a space-free path for the --user-data-dir="pir_profile".Auto-Pinning: The script uses jq to inject extension pinning and visibility settings into pir_profile/Default/Preferences (using manifests/initial_preferences as a template) before launch.Session Hygiene: On every boot, the script wipes Default/Sessions/ and Last Tabs to prevent crashed or stale tabs from reappearing.6. Maintenance & ModdingNative Messaging: Communication is handled via pir_host.js. Ensure the manifest points to the correct absolute path of the launcher script.UI Updates: Modify UI/dashboard.html to update the "The Meadow" startup experience.Binary Updates: Replace bin/aria2c only with certified RPC-enabled forks.Certified by Antigravity — 2026-03-31 ⚓🏴‍☠️🏁
